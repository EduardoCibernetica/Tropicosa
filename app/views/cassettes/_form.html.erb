<%= form_for(cassette) do |f| %>
  <% if cassette.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cassette.errors.count, "error") %> prohibited this cassette from being saved:</h2>

      <ul>
      <% cassette.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Titulo %>
    <%= f.text_field :Titulo %>
  </div>

  <div class="field">
    <%= f.label :Autor %>
    <%= f.text_field :Autor %>
  </div>

  <div class="field">
    <%= f.label :Ano %>
    <%= f.text_field :Ano %>
  </div>

  <div class="field">
    <%= f.label :Disquetera %>
    <%= f.text_field :Disquetera %>
  </div>

  <div class="field">
    <%= f.label :Precio %>
    <%= f.text_field :Precio %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
